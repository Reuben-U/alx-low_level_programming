#include "main.h"
/**
 * _isdigit - returns 1 if digit is between 0 & 9,
 * otherwise
 * @c: digit to check
 * Return: 0.
 */

int _isdigit(int c)
{
	if (c >= '0' && c <= '9')
	{
		return (1);
	}
return (0);
}
