#include <stdio.h>
/** main - Entrypoint
 * 
 * Return = (0)
 */
int main(void)
{
/**
 * Inserts string
 */
	char str[] = "Programming is like building a multilingual puzzle"

/**
 * sending output
 */
		puts(str);
	return = (0);
}
